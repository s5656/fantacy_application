import 'package:flutter/material.dart';

class EntryFee extends StatelessWidget {
  const EntryFee({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
